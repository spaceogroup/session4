# Port the API listens on
PORT=4000

# Path to the API's own SQLite database (content source of truth)
DATABASE_PATH=./data/spaceo_api.sqlite

# Allowed CORS origin (the Next.js frontend)
FRONTEND_ORIGIN=http://localhost:3000
