# The frontend's OWN database (visitor engagement: contact + newsletter).
# Kept entirely separate from the API database.
DATABASE_URL="file:./spaceo_web.sqlite"

# Base URL of the NestJS content API.
NEXT_PUBLIC_API_URL="http://localhost:4000/api/v1"
